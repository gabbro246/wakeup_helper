# Replace these entity IDs with the ones created for your routines.

nap_mode:
  type: custom:wakeup-helper-card
  entity: switch.bedroom_nap

wake_up_light:
  type: custom:wakeup-helper-card
  entity: switch.bedroom_wake_up_light


# Horizontal is the default. Add this to either card for the vertical layout:
# vertical: true
# Or use the icon-and-details-above-controls layout:
# content_layout: features
# If you renamed individual entities, point the card at them explicitly:
# alarm_time_entity: time.my_alarm
# duration_entity: number.my_fade_duration
# status_entity: sensor.my_status
# end_entity: sensor.my_next_alarm
# remaining_entity: sensor.my_remaining
